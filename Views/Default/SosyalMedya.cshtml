@using DbMvc.Models.Entity
@model List<Tbl_SocialMedia>





<div class="social-icons">
    @foreach(var item in Model) {
    <a class="social-icon" href="@item.url"><i class="@item.icon"></i></a>
    }
</div>